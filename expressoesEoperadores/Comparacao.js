    // Operadores de comparação
    // . Irá comparar valroes e retornar um Boolean como resposta à comparação

    let one = 1
    let two = 2

    // = igual
    console.log(one == 1)
    console.log(one == "1")

    // != diferente
    console.log(one != two)
    console.log(one != 1)
    console.log(one != "1")

    // === estritamente igual
    console.log( one === 1 )
    console.log( one === "1" )
    Ex: console.log( "1" + 1 )

    // !== estritamente diferente
    console.log( two === "2")
    console.log( two === 2)

    // > Maior
    console.log(one > two)

    // >= Maior igual
    console.log(one >= 1)
    console.log(two >= 1)

    // < Menor
    console.log(one < two)

    // <= Menor igual
    console.log(one <= two)
    console.log(one <= 1)
    console.log(one <= 0)